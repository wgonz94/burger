{{!-- Routes data by value of devoured or not devoured --}}
<h1>Burgers!</h1>

<h2>Devour the Burger!!</h2>

<ul>
    {{#each burgers}}
    {{#unless devoured}}
     {{!-- code here for placement --}}
    {{/unless}}
    {{/each}}
</ul>

<h2>Burgers Devoured</h2>

<ul>
{{#each burgers}}
{{#if devoured}}
{{!-- code here for placement --}}
{{/if}}
{{/each}}

</ul>
{{!-- Burger creation outline/button --}}
<h2>Create a Burger</h2>
<form class="create-form">

    <div class="form-group">
        <label for="bg">Burger Name:</label>
        <input type="text" id="bg" name="name">
    </div>

    <div class="form-group">
        <label for="devoured">Devoured?</label><br>
        <input type="radio" name="devoured" value="0"> Devoured! <br>
        <input type="radio" name="devoured" value="1" checked> Not yet
    </div>

    <button type="submit">Create Burger</button>
</form>